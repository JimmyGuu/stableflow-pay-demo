# StableFlow hosts (server)
STABLEFLOW_API_BASE=https://test-api.stableflow.ai
STABLEFLOW_PAY_HOST=https://pay.stableflow.ai

# Public demo origin used for success_url
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Demo-only: API key, webhook secret, network, symbol, and recipient
# are configured in the browser UI (not required here).
